# FollowBox 待办任务清单与落地实施

> 数据来源：AI-HANDOFF-MEMORY.md + FOLLOWBOX-VISION-BACKLOG.md + 代码审查 + 用户讨论
> 审查日期：2026-06-12
> 符号说明：✅ 已做完  🟡 部分完成  ⬜ 未开始  ❌ 已关闭

---

## 一、P0 — 本地安全闭环（当前状态）

### 1.1 核心链路（FIRMWARE-SPEC）

| 模块 | 状态 | 文档引用 | 说明 |
|------|:----:|---------|------|
| `main.cpp` 双核任务化 | ✅ | FIRMWARE-SPEC §7.1 | control(Core1) + sensor(Core0) + comm(Core0) |
| `safety_manager` 安全裁决 | ✅ | FIRMWARE-SPEC §4 | applyFinalGate + latched fault |
| `mode_manager` 模式切换 | ✅ | FIRMWARE-SPEC §6 | 8 模式 + 真值表 |
| `command_pipeline` 输入合成 | ✅ | FIRMWARE-SPEC §4 | MotionIntent 输出 |
| `motion_mixer` 差速混控 | ✅ | FIRMWARE-SPEC §4 | forward/turn→左右 |
| `drive_adapter_analog_bldc` 输出 | ✅ | FIRMWARE-SPEC §5.3 | 唯一 PWM 出口 |
| DS600 RC 输入 | ✅ | 协议 DS600-PWM | 5 路中断式PWM |
| H5 本地面板 (LittleFS) | ✅ | 协议 H5-API | WS + POST API |
| UWB GC-P2304 解析器 | ✅ | 协议 UWB-GC-P2304 | 纯逻辑 |
| VL53L1X TOF 阵列 | ✅ | sensor_task | TCA9548A |
| HC-SR04 超声 | ✅ | sensor_task | 共享TRIG |
| JY61P IMU 解析器 | 🟡 | 协议 JY61P | 解析器就绪，UART 未接线（UART_NUM_IMU=-1）|
| 电源监控 ADC | ✅ | sensor_task | 220k/10k分压 |
| 避障限速/停车 | ✅ | obstacle_manager | 前方障碍stop/slow|
| `telemetry_logger` 日志 | ✅ | storage/telemetry | 限速环形缓冲 |
| `debug_console` 调试 | ✅ | storage/telemetry | 分级 printf |
| `calibration_store` 标定 | ✅ | storage | NVS 持久化 |
| `profile_store` 配置 | ✅ | storage | NVS 持久化 |
| `shared_state` 跨核邮箱 | ✅ | FIRMWARE-SPEC §7.1 | portMUX 单槽 |
| 云服务器 Node.js | ✅ | cloud/server.js | REST API + SSE |
| ESP32 云端客户端 | ✅ | cloud_client.cpp | 遥测上传+命令轮询 |
| 云端 H5 面板 | ✅ | cloud/public/ | 实时状态+日志+点动 |
| 云遥测规范 | ✅ | CLOUD-TELEMETRY-SPEC.md | 安全边界完整 |

### 1.2 仍需处理的 P0 问题

| # | 问题 | 严重度 | 位置 | 建议修复 |
|:-:|------|:-----:|------|---------|
| 1 | `main.cpp` 150 行 > 规范 80-120 行 | P1 | `main.cpp` | 任务编排搬到 `app/scheduler.*` |
| 2 | IMU UART_NUM_IMU=-1 实际禁用，但配置文件标记 enabled | P1 | `board_pins.h` + `profile_defaults.h` | 改为 disabled 或启用 UART0→GPIO42 |
| 3 | H5 双目录 drift：`firmware/web/` vs `firmware/data/` | P1 | 两个目录 | 删 `firmware/web/`，只保留 `data/` |
| 4 | POST body 用 static char buf[]，异步请求可能互盖 | P1 | `h5_web_server.cpp` | 改为每请求缓冲或限制并发 |
| 5 | 烟测 `logic_smoke_test.cpp` 只验证 host 编译，缺少硬件在环测试 | P2 | `tools/logic_smoke_test.cpp` | 增加压力测试和边界条件 |
| 6 | 当前 `cloud_config.h` 全为空 URL/token | ⚠️ 阻塞 | `cloud_config.h` | 部署云服务器后填写 |
| 7 | `network_config.h` 无 STA WiFi | ⚠️ 阻塞 | `network_config.h` | 需要先确定 WiFi 环境 |

---

## 二、P1 — 云端遥测 + 远程调试（远景一）

> 见 [02-H5云端遥测控制方案.md](02-H5云端遥测控制方案.md) 详细方案
> 需首烧验证通过后才进行此阶段

| # | 任务 | 优先级 | 状态 | 依赖 | 预估 |
|:-:|------|:------:|:----:|:----:|:----:|
| 1 | 部署云服务器到 `followbox.boonai.cn` | **P1** | ⬜ | 需有服务器 | 1h |
| 2 | 配置 Nginx 反向代理 + SSL | **P1** | ⬜ | 服务器就绪 | 0.5h |
| 3 | 填写 `cloud_config.h` 的 URL/ID/Token | **P1** | ⬜ | 云服务器就绪 | 15min |
| 4 | 填写 `network_config.h` 的 WiFi STA | **P1** | ⬜ | 决定 WiFi | 15min |
| 5 | 编译带 CLOUD_ENABLED 的固件 | **P1** | ⬜ | 3-4 完成 | 30min |
| 6 | 首烧验证 + `uploadfs` | **P1** | ⬜ | 5 完成 | 1h |
| 7 | 验证云端 H5 实时状态 | **P1** | ⬜ | 6 完成 | 30min |
| 8 | 验证云端 deadman 点动 | **P1** | ⬜ | 6 完成 | 30min |
| 9 | 验证日志上传和查看 | **P1** | ⬜ | 6 完成 | 30min |
| 10 | 验证断线自动停车 | **P1** | ⬜ | 6 完成 | 15min |

---

## 三、P2 — OTA 固件升级（远景一·续）

> 见 [01-OTA可行性方案.md](01-OTA可行性方案.md) 详细方案
> 需云端遥测打通后做

| # | 任务 | 优先级 | 状态 | 依赖 | 预估 |
|:-:|------|:------:|:----:|:----:|:----:|
| 1 | 创建 `partitions/ota_8MB.csv` | **P2** | ⬜ | — | 30min |
| 2 | 创建 `include/config/ota_config.h` | **P2** | ⬜ | — | 30min |
| 3 | 实现 `src/ota/ota_manager.h/.cpp` | **P2** | ⬜ | — | 2h |
| 4 | 扩展 `cloud/server.js` 加 version/download API | **P2** | ⬜ | — | 1h |
| 5 | 接入 `main.cpp` comm_task | **P2** | ⬜ | 3 完成 | 30min |
| 6 | 本地先烧 OTA 分区表 | **P2** | ⬜ | 1 完成 | 1h |
| 7 | 测试局域网 OTA | **P2** | ⬜ | 5+6 完成 | 1h |
| 8 | 测试公网 OTA | **P2** | ⬜ | 7 完成 | 1h |
| 9 | 编写 `OTA-UPDATE-SPEC.md` 正式规范 | **P2** | ⬜ | 8 完成 | 1h |

---

## 四、P2 — 室内测试小底盘

> 当云端控制打通后，可以搞一个小底盘专门用于室内测试

| # | 任务 | 优先级 | 状态 | 预估 | 预算 |
|:-:|------|:------:|:----:|:----:|:----:|
| 1 | 采购 4WD 小车底盘 + N20 电机 | **P2** | ⬜ | 快递3天 | ¥110 |
| 2 | 采购 L298N 驱动模块 | **P2** | ⬜ | 快递3天 | ¥20 |
| 3 | 采购 3S 锂电池组 | **P2** | ⬜ | 快递3天 | ¥50 |
| 4 | 组装小底盘+接线 | **P2** | ⬜ | 1h | — |
| 5 | 小底盘使用同一个云服务器 | **P2** | ⬜ | 云端就绪后 | — |
| 6 | 验证小底盘 OTA 烧录 | **P2** | ⬜ | OTA 就绪后 | — |

---

## 五、P2-P3 — 远景二：轨迹记录 → 示教-回放

| # | 任务 | 优先级 | 状态 | 预估 |
|:-:|------|:------:|:----:|:----:|
| 1 | 定义 UWB 轨迹数据格式 | P2 | ⬜ | 1h |
| 2 | 云端轨迹存储/回放接口 | P2 | ⬜ | 2h |
| 3 | ESP32 轨迹点上传（每 100ms） | P2 | ⬜ | 2h |
| 4 | 云端轨迹可视化（H5 画轨迹线） | P2 | ⬜ | 2h |
| 5 | 轨迹回放 REPLAY 模式 | P3 | ⬜ | 4h |
| 6 | 多路线存储/选择 | P4 | ⬜ | 2h |

---

## 六、P2-P3 — 远景三：自主交互

| # | 任务 | 优先级 | 状态 | 预估 | 硬件需求 |
|:-:|------|:------:|:----:|:----:|:---------:|
| 1 | TOF 卡住检测逻辑 (>3s) | P2 | ⬜ | 1h | 已有 |
| 2 | CAM 拍照上传 JPEG | P2 | ⬜ | 2h | 已有 |
| 3 | 云端 VLM 场景判断 | P2 | ⬜ | 3h | 已有 |
| 4 | 买 MAX98357 + 喇叭 | P2 | ⬜ | ¥25 | 需购买 |
| 5 | 预录语音+播放 | P2 | ⬜ | 1h | 需组装 |
| 6 | 双向语音 (INMP441) | P3 | ⬜ | 3h | ¥40 |

---

## 七、立即可以落地的步骤

### 7.1 本周可做的（不需硬件）

1. **部署云服务器到 boonai.cn**
   - 复制 `cloud/` 目录到 `followbox.boonai.cn`
   - Nginx 反向代理
   - PM2 管理

2. **编写 `OTA-UPDATE-SPEC.md`**
   - 版本管理
   - 分区表设计
   - 校验/回滚/安全

3. **合并 `firmware/data/` 和 `firmware/web/`**
   - 删 `web/` 目录
   - 确认所有引用指向 `data/`

### 7.2 需硬件的步骤

4. **本地 USB 首烧验证**（需要 ESP32 + 串口线）
   - 编译 → 烧录 → uploadfs → 验证
   - 必须架空车轮

5. **云端遥测验证**（需要 WiFi + 云服务器）
   - 填写 STA WiFi 和 cloud URL
   - 验证 ingest/log/command

6. **OTA 测试**（需要 4 完成+分区表）
   - 局域网 OTA → 公网 OTA

---

## 八、架构审查问题

### 8.1 已验证通过

| 检查项 | 结果 | 依据 |
|--------|:----:|------|
| Motion 路径唯一经由 safety→mode→pipeline→mixer→applyFinalGate→drive | ✅ | main.cpp 第 87-104 行 |
| 云端不能直接 PWM/清急停/绕安全门控 | ✅ | cloud_client.cpp + CLOUD-TELEMETRY-SPEC.md |
| 双核隔离：motor 只在 Core1 | ✅ | main.cpp controlTask pinned to Core1 |
| 传感器读数和日志不阻塞控制环 | ✅ | Core0 独立任务 |
| NVS 标定持久化 | ✅ | calibration_store.cpp |
| H5 deadman 默认安全 | ✅ | h5_command_handler.cpp |
| WiFi 断线/命令超时停车 | ✅ | cloud_client.cpp pollInput isStale 检查 |

### 8.2 需修复/改进

| 检查项 | 问题 | 严重度 | 建议 |
|--------|------|:-----:|------|
| 分区表不支持 OTA | `default_8MB.csv` 无 dual OTA 分区 | P1 | 创建 `ota_8MB.csv` |
| H5 双源目录 drift | `firmware/web/` vs `firmware/data/` | P1 | 删 `web/` |
| main.cpp 超行 | 150 行 > 目标 80-120 | P2 | 搬到 scheduler |
| IMU 实际禁用 | UART_NUM_IMU=-1 | P2 | 修复配置或实现 UART0→GPIO42 |
| POST body 静态缓冲区 | 异步并发时可能冲突 | P2 | 改为动态或限制并发 |
| 无硬件在环测试 | 只有 host 烟测 | P2 | 增加硬件测试计划 |
| README.md 过期 | 写 "硬件输出模块尚未实现" | P1 | 更新 |
| 烟测只覆盖纯逻辑 | 缺少集成测试 | P2 | 增加 |
