[platformio]
default_envs = esp32-s3-devkitc-1
src_dir = src
boards_dir = ../../boards

[env]
platform = espressif32
board = esp32-s3-devkitc-1-n32r16v
framework = arduino
monitor_speed = 115200
build_flags =
  -D ARDUINO_USB_CDC_ON_BOOT=1
  -std=gnu++17
build_unflags =
  -std=gnu++11

[env:esp32-s3-devkitc-1]
; FollowBox default board:
; ESP32-S3-DevKitC-1-N32R16V / ESP32-S3-WROOM-2, 32 MB Octal flash
; + 16 MB Octal PSRAM, OPI/1.8 V.
board_build.arduino.memory_type = opi_opi
board_build.boot = opi
board_build.flash_mode = dout
board_upload.flash_size = 32MB
board_build.partitions = ../../partitions/ota_32MB.csv
build_flags =
  ${env.build_flags}
  -D BOARD_HAS_PSRAM
